<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.server.mapper.FileMsgMapper">
    <resultMap id="fileMsgResultMap" type="com.server.pojo.FileMsg">
        <result column="message_time" property="messageTime"/>
        <result column="sender_id" property="senderId"/>
        <result column="receive_id" property="receiveId"/>
        <result column="file_address" property="fileAddress"/>
    </resultMap>

    <insert id="addFileMsg">
        insert into tbl_file_msg(message_time, file_address, sender_id, receiver_id)
        values (#{messageTime}, #{fileAddress}, #{senderId}, #{receiveId})
    </insert>

    <select id="selectByReceiveId" resultMap="fileMsgResultMap">
        select *
        from tbl_file_msg
        where receiver_id = #{receiveId}
    </select>


</mapper>
